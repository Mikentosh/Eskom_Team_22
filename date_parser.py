# This is Michael's function

# Function 3: Takes in a date in one format and converts it into another format.
# Must take in a "date" and output a "string" with the correct format
# No extra functions may be used